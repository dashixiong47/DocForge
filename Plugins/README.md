# DocForge Plugins

This directory is for project-provided plugin manifests that are safe to publish with the repository.

Keep private or client-specific plugins in `PrivatePlugins/`; that directory is ignored by git.
