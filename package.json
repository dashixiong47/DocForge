{
  "name": "docforge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute docforge-db --local --file=./migrations/0001_initial.sql && wrangler d1 execute docforge-db --local --file=./migrations/0002_translations.sql && wrangler d1 execute docforge-db --local --file=./migrations/0003_media_placeholder_key.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.3",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250303.0",
    "drizzle-kit": "^0.30.4",
    "playwright": "^1.60.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.1"
  }
}
